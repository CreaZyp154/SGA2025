using System.Collections.Generic;
using UnityEngine;

public class PlayerEffectControler : MonoBehaviour
{
    //private List<Modifier> activeEffects = new List<Modifier>(); 

    /*public void ActivateEffect(Potion potion)
    {
        foreach (Effect effect in potion.effects)
        {
            activeEffects.Add(effect);
        }
    }

    private void ApplyEffects()
    {
        foreach (Effect effect in activeEffects)
        {
            switch (effect)
            {
                case Effect.InvertControls:
                    InvertControls();
                    break;
            }
        }
    }*/

    //private void InvertControls()
    //{
    //    GetComponent<player>().speedModifier *= -1.0f;
    //}
}
